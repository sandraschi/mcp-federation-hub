import React from 'react';
import { motion } from 'framer-motion';
import {
    Brain,
    Cpu,
    Zap,
    Activity,
    Terminal,
    Box,
    Database,
    Sparkles,
    ArrowUpRight,
    Gauge,
    Bot
} from 'lucide-react';
import {
    AreaChart,
    Area,
    XAxis,
    YAxis,
    CartesianGrid,
    Tooltip,
    ResponsiveContainer
} from 'recharts';
import { cn } from '@/lib/utils';

const Intelligence: React.FC = () => {
    const gpuData = [
        { name: '10:00', load: 45, vram: 12 },
        { name: '10:05', load: 52, vram: 14 },
        { name: '10:10', load: 48, vram: 13 },
        { name: '10:15', load: 85, vram: 18 },
        { name: '10:20', load: 92, vram: 22 },
        { name: '10:25', load: 65, vram: 16 },
        { name: '10:30', load: 42, vram: 12 },
    ];

    const models = [
        { name: 'Gemma 3 27B', provider: 'Ollama (Local)', status: 'Active', vram: '16GB', latency: '42ms' },
        { name: 'Llama 3.3 70B', provider: 'Ollama (Local)', status: 'Standby', vram: '24GB', latency: '120ms' },
        { name: 'GPT-4o', provider: 'OpenAI (Cloud)', status: 'Ready', vram: 'N/A', latency: '850ms' },
    ];

    return (
        <div className="space-y-8 animate-in">
            {/* Header */}
            <div className="flex flex-col md:flex-row md:items-center justify-between gap-6">
                <div>
                    <h1 className="text-4xl font-outfit font-bold tracking-tight gradient-text">Intelligence Hub</h1>
                    <p className="text-slate-400 mt-1">Localized LLM orchestration and GPU compute telemetry.</p>
                </div>

                <div className="flex items-center gap-4 px-6 py-3 sota-card bg-blue-500/[0.03] border-blue-500/20">
                    <div className="text-right">
                        <span className="block text-[10px] font-bold uppercase tracking-widest text-slate-500 mb-1">Local GPU</span>
                        <span className="text-sm font-bold text-blue-400 uppercase tracking-widest tracking-tighter">RTX 4090 • NOMINAL</span>
                    </div>
                    <div className="w-10 h-10 rounded-full bg-blue-500/10 flex items-center justify-center">
                        <Cpu className="w-5 h-5 text-blue-400" />
                    </div>
                </div>
            </div>

            {/* Compute Overview */}
            <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
                <div className="sota-card p-6 border-blue-500/10 group">
                    <div className="flex items-center justify-between mb-4">
                        <div className="p-2 rounded-xl bg-blue-500/10 text-blue-400">
                            <Gauge size={20} />
                        </div>
                        <span className="text-[10px] font-bold uppercase tracking-widest text-slate-500">Utilization</span>
                    </div>
                    <h3 className="text-3xl font-bold font-outfit mb-2">42% <span className="text-sm font-normal text-slate-500">Core</span></h3>
                    <div className="w-full h-1 bg-white/5 rounded-full overflow-hidden">
                        <motion.div initial={{ width: 0 }} animate={{ width: '42%' }} className="h-full bg-blue-500 shadow-[0_0_10px_rgba(56,189,248,0.5)]" />
                    </div>
                </div>

                <div className="sota-card p-6 border-amber-500/10 group">
                    <div className="flex items-center justify-between mb-4">
                        <div className="p-2 rounded-xl bg-amber-500/10 text-amber-400">
                            <Database size={20} />
                        </div>
                        <span className="text-[10px] font-bold uppercase tracking-widest text-slate-500">VRAM Pressure</span>
                    </div>
                    <h3 className="text-3xl font-bold font-outfit mb-2">18.4 <span className="text-sm font-normal text-slate-500">GB / 24GB</span></h3>
                    <div className="w-full h-1 bg-white/5 rounded-full overflow-hidden">
                        <motion.div initial={{ width: 0 }} animate={{ width: '76%' }} className="h-full bg-amber-500 shadow-[0_0_10px_rgba(245,158,11,0.5)]" />
                    </div>
                </div>

                <div className="sota-card p-6 border-indigo-500/10 group">
                    <div className="flex items-center justify-between mb-4">
                        <div className="p-2 rounded-xl bg-indigo-500/10 text-indigo-400">
                            <Brain size={20} />
                        </div>
                        <span className="text-[10px] font-bold uppercase tracking-widest text-slate-500">Token Flux</span>
                    </div>
                    <h3 className="text-3xl font-bold font-outfit mb-2">1.2k <span className="text-sm font-normal text-slate-500">TPS</span></h3>
                    <div className="w-full h-1 bg-white/5 rounded-full overflow-hidden">
                        <motion.div initial={{ width: 0 }} animate={{ width: '65%' }} className="h-full bg-indigo-500 shadow-[0_0_10px_rgba(99,102,241,0.5)]" />
                    </div>
                </div>
            </div>

            <div className="grid grid-cols-1 lg:grid-cols-5 gap-8">
                {/* Experimental Core */}
                <div className="lg:col-span-3 sota-card p-8 border-cyan-500/10 bg-cyan-500/[0.01]">
                    <div className="flex items-center justify-between mb-8">
                        <div className="flex items-center gap-3">
                            <Sparkles size={20} className="text-cyan-400" />
                            <h3 className="text-lg font-bold font-outfit">Experimental Core Telemetry</h3>
                        </div>
                        <span className="text-[10px] font-bold uppercase tracking-widest text-cyan-500/50">Phase 5 (Active)</span>
                    </div>

                    <div className="grid grid-cols-1 md:grid-cols-2 gap-8">
                        <div className="space-y-6">
                            <div>
                                <div className="flex justify-between items-end mb-2">
                                    <span className="text-xs font-bold text-slate-400 uppercase tracking-widest">Neural Sync (Mind Uploading)</span>
                                    <span className="text-xs font-bold text-fuchsia-400 mono">84.2%</span>
                                </div>
                                <div className="w-full h-1.5 bg-white/5 rounded-full overflow-hidden">
                                    <motion.div
                                        initial={{ width: 0 }}
                                        animate={{ width: '84.2%' }}
                                        transition={{ duration: 1.5, ease: "easeOut" }}
                                        className="h-full bg-gradient-to-r from-fuchsia-500 to-indigo-500 shadow-[0_0_15px_rgba(217,70,239,0.5)]"
                                    />
                                </div>
                                <p className="text-[9px] text-slate-500 mt-2 italic">Synaptic mapping in progress. 4.2 petabytes remaining.</p>
                            </div>

                            <div>
                                <div className="flex justify-between items-end mb-2">
                                    <span className="text-xs font-bold text-slate-400 uppercase tracking-widest">FTL Data Bus Saturation</span>
                                    <span className="text-xs font-bold text-cyan-400 mono">12.8%</span>
                                </div>
                                <div className="w-full h-1.5 bg-white/5 rounded-full overflow-hidden">
                                    <motion.div
                                        initial={{ width: 0 }}
                                        animate={{ width: '12.8%' }}
                                        transition={{ duration: 1.5, ease: "easeOut" }}
                                        className="h-full bg-cyan-400 shadow-[0_0_15px_rgba(34,211,238,0.5)]"
                                    />
                                </div>
                                <p className="text-[9px] text-slate-500 mt-2 italic">Zero-latency tunnel established. Signal integrity: 0.99998.</p>
                            </div>
                        </div>

                        <div className="p-6 rounded-2xl bg-white/[0.02] border border-white/5 flex flex-col justify-center items-center text-center space-y-4 overflow-hidden relative">
                            <div className="absolute inset-0 opacity-20 pointer-events-none">
                                <img
                                    src="file:///C:/Users/sandr/.gemini/antigravity/brain/930cc217-af01-4c3e-b94a-fe5f922729d5/nano_banana_neural_sync_1772238458329.png"
                                    alt="Nano Banana Sync"
                                    className="w-full h-full object-cover grayscale brightness-200"
                                />
                            </div>
                            <div className="w-16 h-16 rounded-2xl overflow-hidden border border-cyan-500/30 relative z-10 shadow-[0_0_20px_rgba(34,211,238,0.2)]">
                                <img
                                    src="file:///C:/Users/sandr/.gemini/antigravity/brain/930cc217-af01-4c3e-b94a-fe5f922729d5/nano_banana_ftl_core_1772238471714.png"
                                    alt="Nano Banana FTL"
                                    className="w-full h-full object-cover"
                                />
                            </div>
                            <div className="relative z-10">
                                <h4 className="text-sm font-bold text-slate-200">Quantum Handshake Staging</h4>
                                <p className="text-[10px] text-slate-500 mt-1 italic tracking-tighter">"Mindblowing functionality in progress"</p>
                            </div>
                        </div>
                    </div>
                </div>

                {/* GPU Trace */}
                <div className="lg:col-span-3 sota-card p-8">
                    <div className="flex items-center justify-between mb-8">
                        <div className="flex items-center gap-3">
                            <Activity size={20} className="text-blue-500" />
                            <h3 className="text-lg font-bold font-outfit">Compute Telemetry Trace</h3>
                        </div>
                        <div className="flex items-center gap-2">
                            <Box size={14} className="text-slate-500" />
                            <span className="text-xs font-bold text-slate-500 uppercase tracking-tighter">Live Stream (5m window)</span>
                        </div>
                    </div>

                    <div className="h-[320px] w-full">
                        <ResponsiveContainer width="100%" height="100%">
                            <AreaChart data={gpuData}>
                                <defs>
                                    <linearGradient id="gpuLoad" x1="0" y1="0" x2="0" y2="1">
                                        <stop offset="5%" stopColor="#3b82f6" stopOpacity={0.2} />
                                        <stop offset="95%" stopColor="#3b82f6" stopOpacity={0} />
                                    </linearGradient>
                                </defs>
                                <CartesianGrid strokeDasharray="3 3" stroke="rgba(255,255,255,0.03)" vertical={false} />
                                <XAxis dataKey="name" axisLine={false} tickLine={false} tick={{ fill: '#475569', fontSize: 10 }} dy={10} />
                                <YAxis hide />
                                <Tooltip
                                    contentStyle={{ backgroundColor: '#0a0a0f', border: '1px solid rgba(255,255,255,0.1)', borderRadius: '12px', fontSize: '11px' }}
                                    itemStyle={{ color: '#fff' }}
                                />
                                <Area type="step" dataKey="load" stroke="#3b82f6" strokeWidth={2} fillOpacity={1} fill="url(#gpuLoad)" animationDuration={2000} />
                            </AreaChart>
                        </ResponsiveContainer>
                    </div>
                </div>

                {/* Local Model Registry */}
                <div className="lg:col-span-2 sota-card p-6 border-white/10">
                    <div className="flex items-center justify-between mb-6">
                        <div className="flex items-center gap-3">
                            <Bot size={20} className="text-indigo-400" />
                            <h3 className="text-lg font-bold font-outfit">Intelligence Registry</h3>
                        </div>
                        <Sparkles size={18} className="text-amber-500 animate-pulse" />
                    </div>

                    <div className="space-y-4">
                        {models.map((model, i) => (
                            <div key={i} className="p-4 rounded-2xl bg-white/[0.02] border border-white/5 hover:bg-white/[0.05] transition-all group cursor-pointer">
                                <div className="flex items-center justify-between mb-2">
                                    <div className="flex items-center gap-3">
                                        <div className="w-8 h-8 rounded-lg bg-indigo-500/10 flex items-center justify-center text-indigo-400">
                                            <Zap size={16} />
                                        </div>
                                        <h4 className="text-sm font-bold text-slate-100 group-hover:text-blue-400 transition-colors">{model.name}</h4>
                                    </div>
                                    <span className={cn(
                                        "text-[9px] font-bold px-1.5 py-0.5 rounded uppercase",
                                        model.status === 'Active' ? "bg-emerald-500/10 text-emerald-400" : "bg-slate-500/10 text-slate-500"
                                    )}>
                                        {model.status}
                                    </span>
                                </div>
                                <div className="grid grid-cols-2 gap-4 mt-3 pt-3 border-t border-white/5">
                                    <div className="flex flex-col">
                                        <span className="text-[9px] uppercase font-bold text-slate-500 tracking-widest">Provider</span>
                                        <span className="text-[11px] font-medium text-slate-300">{model.provider}</span>
                                    </div>
                                    <div className="flex flex-col text-right">
                                        <span className="text-[9px] uppercase font-bold text-slate-500 tracking-widest">Latency</span>
                                        <span className="text-[11px] font-medium text-slate-300 mono">{model.latency}</span>
                                    </div>
                                </div>
                            </div>
                        ))}
                    </div>

                    <button className="w-full mt-8 py-3 rounded-2xl bg-white/[0.03] border border-white/10 text-xs font-bold uppercase tracking-widest text-slate-400 hover:bg-white/[0.06] hover:text-white transition-all flex items-center justify-center gap-2">
                        <Terminal size={14} />
                        Open Ollama Local Console
                    </button>
                </div>
            </div>
        </div>
    );
};

export default Intelligence;
