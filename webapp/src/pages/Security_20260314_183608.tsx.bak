import React from 'react';
import { motion } from 'framer-motion';
import {
    Shield,
    Lock,
    AlertCircle,
    Fingerprint,
    Activity,
    Globe,
    Eye,
    Server,
    Zap,
    Terminal,
    ArrowUpRight,
    UserX
} from 'lucide-react';
import { cn } from '@/lib/utils';

const Security: React.FC = () => {
    const threats = [
        { id: 1, type: 'Brute Force', origin: '192.168.1.45', status: 'Blocked', severity: 'High', time: '2m ago' },
        { id: 2, type: 'Scanning', origin: '172.16.0.12', status: 'Isolated', severity: 'Med', time: '15m ago' },
        { id: 3, type: 'Policy Violation', origin: 'Admin Session', status: 'Logged', severity: 'Low', time: '1h ago' },
    ];

    const sessions = [
        { user: 'Sandra', node: 'Vienna Core', status: 'Authorized', level: 'Admin', time: 'Active' },
        { user: 'Backup-Bot', node: 'London-BC', status: 'Authorized', level: 'Service', time: '4h ago' },
    ];

    return (
        <div className="space-y-8 animate-in">
            {/* Header */}
            <div className="flex flex-col md:flex-row md:items-center justify-between gap-6">
                <div>
                    <h1 className="text-4xl font-outfit font-bold tracking-tight gradient-text">Security Command</h1>
                    <p className="text-slate-400 mt-1">Real-time threat detection and bastion access control. Hub-to-hub mesh links use HTTPS and optional Bearer tokens (see Peers).</p>
                </div>

                <div className="flex items-center gap-4 px-6 py-3 sota-card bg-rose-500/[0.03] border-rose-500/20">
                    <div className="text-right">
                        <span className="block text-[10px] font-bold uppercase tracking-widest text-slate-500 mb-1">Threat Level</span>
                        <span className="text-sm font-bold text-rose-400 uppercase tracking-widest">Minimal / Alpha</span>
                    </div>
                    <div className="w-10 h-10 rounded-full bg-rose-500/10 flex items-center justify-center">
                        <Shield className="w-5 h-5 text-rose-500" />
                    </div>
                </div>
            </div>

            {/* Security Overview Grid */}
            <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
                {[
                    { label: 'Active Bastions', val: '2', icon: Lock, color: 'text-blue-400' },
                    { label: 'Denied Requests', val: '432', icon: UserX, color: 'text-rose-400' },
                    { label: 'Auth Success', val: '99.2%', icon: Fingerprint, color: 'text-emerald-400' },
                    { label: 'Security Entropy', val: '1.2b', icon: Activity, color: 'text-amber-400' },
                ].map((stat, i) => (
                    <div key={i} className="sota-card p-6">
                        <div className="sota-card-glow" />
                        <div className="flex items-center gap-4 mb-3">
                            <div className={cn("p-2.5 rounded-xl bg-white/5", stat.color)}>
                                <stat.icon size={20} />
                            </div>
                            <span className="text-[10px] font-bold uppercase tracking-[0.2em] text-slate-500">{stat.label}</span>
                        </div>
                        <div className="text-3xl font-bold font-outfit">{stat.val}</div>
                    </div>
                ))}
            </div>

            <div className="grid grid-cols-1 lg:grid-cols-5 gap-8">
                {/* Threat Map / Visualization Placeholder */}
                <div className="lg:col-span-3 space-y-6">
                    <div className="sota-card p-8 aspect-video relative flex items-center justify-center overflow-hidden border-rose-500/10 bg-rose-500/[0.01]">
                        <div className="absolute inset-0 opacity-20 pointer-events-none">
                            <div className="absolute top-1/4 left-1/4 w-32 h-32 bg-rose-500/20 rounded-full blur-3xl animate-pulse" />
                            <div className="absolute bottom-1/4 right-1/4 w-48 h-48 bg-blue-500/20 rounded-full blur-3xl animate-pulse" />
                        </div>
                        <div className="relative text-center space-y-4">
                            <div className="w-20 h-20 mx-auto rounded-full bg-rose-500/10 border border-rose-500/20 flex items-center justify-center holographic-pulse">
                                <Globe className="w-10 h-10 text-rose-400/80" />
                            </div>
                            <div>
                                <h3 className="text-xl font-bold font-outfit text-white">Global Threat Matrix</h3>
                                <p className="text-xs text-slate-500 mono">Visualizing real-time intrusion attempts...</p>
                            </div>
                            <button className="px-6 py-2 rounded-xl bg-white/5 border border-white/10 text-xs font-bold uppercase tracking-widest hover:bg-white/10 transition-all">
                                Launch Detailed Scanner
                            </button>
                        </div>
                    </div>

                    <div className="sota-card p-6">
                        <div className="flex items-center justify-between mb-6">
                            <h3 className="text-lg font-bold font-outfit">Intrusion Logs</h3>
                            <button className="text-xs font-bold text-blue-400 hover:text-blue-300">Export SOTA Trace</button>
                        </div>
                        <div className="space-y-3">
                            {threats.map(threat => (
                                <div key={threat.id} className="flex items-center justify-between p-4 rounded-xl bg-white/[0.02] border border-white/5 hover:border-rose-500/20 transition-all group">
                                    <div className="flex items-center gap-4">
                                        <div className={cn(
                                            "p-2 rounded-lg bg-white/5",
                                            threat.severity === 'High' ? "text-rose-500" : "text-amber-500"
                                        )}>
                                            <AlertCircle size={18} />
                                        </div>
                                        <div>
                                            <h4 className="text-sm font-bold text-slate-200">{threat.type}</h4>
                                            <p className="text-[10px] text-slate-500 mono">Origin: {threat.origin} • {threat.time}</p>
                                        </div>
                                    </div>
                                    <div className="flex items-center gap-4">
                                        <span className={cn(
                                            "text-[10px] font-bold px-2 py-0.5 rounded uppercase tracking-tighter",
                                            threat.status === 'Blocked' ? "bg-rose-500/10 text-rose-400" : "bg-blue-500/10 text-blue-400"
                                        )}>
                                            {threat.status}
                                        </span>
                                        <ArrowUpRight size={14} className="text-slate-600 group-hover:text-white transition-colors cursor-pointer" />
                                    </div>
                                </div>
                            ))}
                        </div>
                    </div>
                </div>

                {/* Bastion & Session Control */}
                <div className="lg:col-span-2 space-y-6">
                    <div className="sota-card p-6 h-full flex flex-col">
                        <div className="flex items-center gap-3 mb-6">
                            <Lock size={18} className="text-blue-500" />
                            <h3 className="text-lg font-bold font-outfit uppercase tracking-tighter">Bastion Access Control</h3>
                        </div>

                        <div className="space-y-6 flex-1">
                            <div className="p-4 rounded-2xl bg-blue-500/5 border border-blue-500/10 space-y-3">
                                <div className="flex items-center justify-between">
                                    <span className="text-xs font-bold text-blue-400 uppercase tracking-widest">Vienna Bastion</span>
                                    <div className="w-3 h-3 rounded-full bg-emerald-500 animate-pulse" />
                                </div>
                                <div className="flex items-center justify-between text-[10px] text-slate-500 mono">
                                    <span>Total Traffic</span>
                                    <span className="text-slate-300 font-bold">4.2 TB/hr</span>
                                </div>
                                <div className="w-full h-1 bg-white/5 rounded-full overflow-hidden">
                                    <div className="w-[65%] h-full bg-blue-500/60" />
                                </div>
                            </div>

                            <div className="space-y-4">
                                <h4 className="text-[10px] font-bold uppercase tracking-widest text-slate-500">Live Sessions</h4>
                                {sessions.map((session, i) => (
                                    <div key={i} className="flex items-center justify-between p-3 rounded-xl bg-white/[0.02] border border-white/5">
                                        <div className="flex items-center gap-3">
                                            <div className="w-8 h-8 rounded-full bg-slate-800 flex items-center justify-center text-blue-400 border border-white/5">
                                                <Fingerprint size={16} />
                                            </div>
                                            <div>
                                                <p className="text-xs font-bold text-slate-200">{session.user}</p>
                                                <p className="text-[9px] text-slate-500 mono uppercase tracking-tighter">{session.node}</p>
                                            </div>
                                        </div>
                                        <div className="text-right">
                                            <span className="block text-[9px] font-bold text-emerald-400/80 uppercase mb-0.5">{session.status}</span>
                                            <span className="text-[10px] text-slate-600 mono">{session.time}</span>
                                        </div>
                                    </div>
                                ))}
                            </div>
                        </div>

                        <div className="mt-8 space-y-3 pt-6 border-t border-white/5">
                            <button className="w-full py-2.5 rounded-xl bg-rose-500/10 text-rose-400 text-xs font-bold uppercase tracking-widest border border-rose-500/20 hover:bg-rose-500/20 transition-all flex items-center justify-center gap-2">
                                <UserX size={14} />
                                Revoke All Session Access
                            </button>
                            <button className="w-full py-2.5 rounded-xl bg-blue-500/10 text-blue-400 text-xs font-bold uppercase tracking-widest border border-blue-500/20 hover:bg-blue-500/20 transition-all">
                                Generate New API Keys
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    );
};

export default Security;
