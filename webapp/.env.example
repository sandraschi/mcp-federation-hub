# Optional: direct bridge URL (skips Vite proxy). Use when UI and API are on different origins in dev.
# VITE_API_URL=http://127.0.0.1:10857

# Optional: where Vite should proxy /api, /health, /docs (default http://127.0.0.1:10857)
# VITE_BRIDGE_PROXY_TARGET=http://127.0.0.1:10857
